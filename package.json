{
  "name": "lettfaktura-sow-terms",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "cd api && npm install && npm start",
    "api:dev": "cd api && npm install && npm run dev",
    "start": "concurrently \"npm run api:dev\" \"npm run dev\"",
    "setup": "cd api && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^7.1.2"
  }
}
